# P6 技能蒸馏 · 蒸馏工作法「归属哪个 skill」—— 已解决 / 归档

> 状态：**已解决（RESOLVED）· 本笔记归档**。主草案 [`../../P6-技能蒸馏-草案.md`](../../P6-技能蒸馏-草案.md) 已拍板。**后续实现按主草案，勿再按本笔记旧路线扩 `skill.py`/`pyproject`。**

## 结论（覆盖本笔记原「未决 / 倾向 B」）

- **工作法归属：选 Option A**——蒸馏工作法**扩既有 `guanlan-wiki` skill** 加一个 `distill` 工作流，**不**新发布 `guanlan-distill` skill（主草案 §6 / 决策 D9）。理由：MVP 不卡在安装/激活面——`SKILL_NAME=guanlan-wiki` 强激活门禁、`BUNDLED_SKILL_NAMES`、`pyproject` force-include 均**零改动**。posture 隔离的专用 skill 留 P6.x。**本笔记原「倾向 B」作废**；Option B 要改 `runtime.py`/`skill.py`/force-include + 定激活方式子决策，正是「不要过度设计」要避开的扩面。
- **marketplace 不在 P6**：收录/分发（含 schema 调研）整块移到 backlog `P6-MKT`、与 P6 排期解耦、**非 P6 前置**（主草案 §7 / 决策 D10）。本笔记背景里写的「LLM 蒸馏 → 校验 → host 写 marketplace **三段**」已收窄为**两段**（蒸馏 + 零-LLM 校验）。
- **快照不重构 gate**：raw/ 直接**复用** `gate.snapshot_raw`/`diff_raw`（只调用、不改）；wiki/config 用轻量只读 helper（主草案 §4 / 决策 D6）。本笔记末尾提到的「零-behavior-change 的 `snapshot_subtree` 重构」**已弃**——不动保命码、也不复刻其防绕过遍历。
- **召回入口收敛**：由 wrapper 先调 `search.search_pages()` 把候选页注入 prompt，**不依赖 host tool、不嵌套 `query_wiki`**（CLI 子进程没有 Web `guanlan_search` 注入面，主草案 §6 / 决策 D2）。

## 仍需的收尾（配套 Option A）

- 真进实现时在 `references/conventions.md` / `examples/SCHEMA.md` 记一笔：「`dist/` 是 distill 产物区、勿当 wiki 维护手改」。

（原「未决项 / 两选项对比 / 拍板后收尾 / 为何不现在拍」内容已被上述结论取代，不再保留——避免后续实现误按 Option B + marketplace 旧路线扩安装面。P6 仍**暂缓进实现**，见主草案状态。）
