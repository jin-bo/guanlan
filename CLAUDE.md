# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

观澜 (GuānLán) is an implementation of the [Karpathy LLM Wiki pattern](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f): an Agent incrementally builds and maintains a structured, cross-linked markdown knowledge wiki instead of doing fresh RAG retrieval on every query. The full design (in Chinese) is the authoritative spec — read [`docs/DESIGN.md`](docs/DESIGN.md) before any non-trivial change.

**Current status: P4 + half-phases through P4.10 plus P5.0/P5.1 retrieval, released as v0.1.6 (P5.0 retrieval layer `guanlan search` + P5.1 Web 检索接入); v0.1.7 in prep — adds the P4.10 MCP host (`guanlan mcp`) (earlier milestones: v0.1.5 = P4.9 read-only multi-conversation `guanlan web --reader`; v0.1.4 lowered `requires-python` to >=3.10). P5.0 retrieval layer (`guanlan search`) **and P5.1 Web 检索接入** are now **implemented** (P5.0: zero-LLM deterministic BM25 + CJK-2-gram full-page recall — `guanlan/search.py` + `search` subcommand + query/skill recall front-end; P5.1: `GET /api/search` + frontend full-text result UI + the embedded-chat `guanlan_search` read-only host tool, all sharing one process-resident `CorpusCache` on `app.state`); P4.10 MCP host is now **also implemented** (`guanlan mcp`: a read-only MCP server over stdio, `guanlan/mcp/` subpackage gated by the `guanlan-wiki[mcp]` extra); P3.5 graph analytics (zero-LLM deterministic Louvain communities + topology lint, `guanlan/graphstats.py`) is now **also implemented**; no further roadmap spec remains unimplemented — see the spec list below.** P2's closed loop (`guanlan init` / `ingest` / `query` / `check` / `install-skill`, wired through Agentao) + P3's three on-demand zero-LLM maintenance tools (`guanlan health` / `lint` / `graph`) + P4's **optional** Web host (`guanlan web`, needs `pip install 'guanlan-wiki[web]'`) are all implemented. P4 puts the existing commands in a browser via a thin FastAPI/uvicorn layer (127.0.0.1-only, `workers=1`) under a `guanlan/web/` subpackage that carries *no business intelligence*: it reuses `run_ingest` / `run_check` / `run_health` / `run_lint` / `build_and_write_graph` / `pages.*` and an embedded read-only `Agentao` for chat. **Read/write split** (DESIGN §5.2): the only write job `ingest` reuses the P2 subprocess + single-writer gate (one background worker, FIFO); all Q&A (one-shot + multi-turn) goes through a read-only in-process embedded `Agentao` (`.arun`, token-streamed, memory-only). Web-side `raw/` writes (P4.1 paste / P4.6 promote), writable work-sessions (P4.5), session persistence (P4.2), a pure-frontend bilingual UI toggle (P4.7), and Web-side `query --backfill` (P4.8) are now implemented as half-phases; multi-format auto-ingest remains post-P4 / P5 (DESIGN §8). When adding features, match the phase boundaries in DESIGN §4.4 / §7.

The **P2 spec** (module layout, deterministic gate, `raw/` snapshot + `check` contracts, exit codes, Agentao integration) is [`docs/P2-最小闭环.md`](docs/P2-最小闭环.md); the **P3 spec** (`pages.py` shared primitives with strict/lenient frontmatter tiers, `graph`/`health`/`lint` contracts, `EXIT_LINT_FINDINGS`, advisory-not-gate exit semantics) is [`docs/P3-健康与图谱.md`](docs/P3-健康与图谱.md); the **P4 spec** (`guanlan/web/` layout, the read/write split, single-worker `ingest` job, read-only embedded chat with the four embedding pitfalls + token-streaming transport contract, HTTP API + SSE contracts, no new exit codes) is [`docs/P4-Web宿主.md`](docs/P4-Web宿主.md) — together they document how the current code is structured. The **P3.1 spec** (optional `aliases` frontmatter feeding the single `pages.py` resolution point: `alias_index` / `link_resolution_index` / alias-aware `link_target_stems`, `check` uniqueness validation `aliases.collides_stem`/`aliases.duplicate`, alias edges in `graph`, Web `[[alias]]` linking — a zero-LLM half-phase enhancement, not a new milestone; P5 still = multi-format) is [`docs/P3.1-别名解析.md`](docs/P3.1-别名解析.md). Two **landing designs for P4 §10 deferred items** (**both implemented**) are [`docs/P4.1-Web投喂.md`](docs/P4.1-Web投喂.md) (**P4.1** — `POST /api/raw` paste-to-save: a human source-add, *not* a gated agent write; serialized through the existing single-writer `JobQueue` to avoid the `run_guarded_write` `raw/` snapshot window; default no-overwrite, slug + `.md`) and [`docs/P4.2-会话落盘.md`](docs/P4.2-会话落盘.md) (**P4.2** — session persistence/restore: reuse `agentao.embedding.{save_session,load_session,list_sessions,delete_session}` under `<kb>/.agentao/sessions/`, conversation id → agentao UUID, lazy restore that re-applies the read-only two-point posture; thin adaptation over agentao's *snapshot-file* semantics — dedup the catalog by `session_id` and prune to one snapshot per session *before* each save (since `save_session` rotates by file-count right after writing) so the 10-*file* rotation cap behaves as a 10-*session* cap). Both are zero-LLM Web-host half-phases that stay inside the P4 boundary (no new exit codes, no SSE changes, no writable sessions). A third Web-host half-phase, [`docs/P4.3-Web-heal.md`](docs/P4.3-Web-heal.md) (**P4.3** — implemented), lands the Web-heal item deferred in P3.2 §11 / P3.3 §10: a read-only `GET /api/heal/preview` (zero-LLM worklist) plus a `POST /api/heal` write job that `enqueue`s heal as a new job kind on the existing single-writer FIFO `JobQueue` (serialized with `ingest`/raw-write, polled via `/api/jobs/{id}`). It carries heal's structured `HealResult` by an additive `heal.py` refactor — a non-printing core `run_heal_result(...) -> HealRun` (mirroring P3.2's `run_guarded_write_result` split; `run_heal` stays a byte-identical CLI shell) — plus an optional `Job.result` field; heal uses the subprocess runner (so the P4-8 embedding pitfalls don't apply), adds no new exit codes/SSE, and never writes `raw/`. The **P3.4 spec** (`docs/P3.4-索引回填.md`, **implemented**) adds `guanlan reindex` — the zero-LLM deterministic *fixer* paired with `health.index_missing_page` (as `heal` pairs with `lint.missing_entity`, but zero-LLM since registering an existing page needs no generation): it sinks the index↔disk sync detection into a single `pages.index_sync_state`归口 (shared by `health` and `reindex`, no drift), then registers each unindexed content page into its `index.md` section (dir→section, frontmatter `title` anchor, `aliases` tail-note; the only LLM-shaped ingredient — the prose one-line summary — is left blank for a later `ingest`/human, decision P3.4-1). Default applies; `--dry-run` previews, `--prune` removes `index_dangling` lines (off by default). Only writes `wiki/index.md` (config catalog) — no `raw/`, no Agentao, no gate, no `log.md`, no new exit codes. Three further Web-host half-phases (**all implemented**) extend the embedded chat: [`docs/P4.4-Web斜杠命令.md`](docs/P4.4-Web斜杠命令.md) (**P4.4** — Web slash-commands + read-only introspection `/status` `/context` `/skills` `/tools` `/mode`, stop button, SSE `start`·`stopped`), [`docs/P4.5-可写Web工作会话.md`](docs/P4.5-可写Web工作会话.md) (**P4.5** — writable work-session `/mode workspace-write` with a three-layer write guard + shared-lock single-writer + undo; Agent may write `workspace/`, `raw/` stays hard read-only), and [`docs/P4.6-Web上传与晋级.md`](docs/P4.6-Web上传与晋级.md) (**P4.6** — `POST /api/upload` staging to `workspace/uploads/`, dual-use as chat `<attachment>` / image-vision *or* a parse→human-review→promote path that writes `raw/`; plus `workspace/` browse/preview/delete endpoints). P4.6 also ships the auxiliary **`pdf-to-markdown` skill** (`skills/pdf-to-markdown/`, force-included into the wheel and installed globally by `guanlan/skill.py` alongside `guanlan-wiki`) so the writable-session Agent can parse uploaded PDF/DOCX/… into `workspace/parsed/` (the multi-format auto-parse pipeline itself is still P5). A seventh Web-host half-phase, [`docs/P4.7-中英双语.md`](docs/P4.7-中英双语.md) (**P4.7** — implemented), adds a top-right language toggle (中文 ⇄ English) over a **pure-frontend, zero-LLM, zero-backend** i18n layer in `static/`: a `static/i18n.js` `{zh,en}` catalog + `t(key,…)`, declarative `data-i18n*` annotations applied by `applyI18n`, `localStorage["guanlan.lang"]` persistence (default zh, no navigator probing). It translates **only interface chrome** — wiki content, agent answers, `check/health/lint` report bodies and `/status`-style introspection are content/diagnostics and stay in their source language (no `lang` param to any endpoint). Switching re-applies static nodes + pure-re-renders the few open dynamic surfaces (Wiki view, open overlay via an `overlayRepaint` closure, attach chips) from cached data — **never reloads, never re-fetches** (`renderPage`→`paintPage`/`repaintPageChrome`; openers cache their data). Guarded by `tests/test_web_i18n.py` (zh/en key parity, HTML/JS `t(...)` key-defined, literal-first-arg rule with the `applyI18n` dataset whitelist); no new endpoints/exit codes/SSE/deps. An eighth Web-host half-phase, [`docs/P4.8-Web回填.md`](docs/P4.8-Web回填.md) (**P4.8** — implemented), lands the last deferred P4 §10 write entry, **`query --backfill`** (gated write of a synthesis): a `POST /api/backfill {question, model?}` write job that `enqueue`s `backfill` as a new job kind on the existing single-writer FIFO `JobQueue` (serialized with `ingest`/`heal`/raw-write, polled via `/api/jobs/{id}`). It **wholesale-reuses** `run_query(question, backfill=True, …)` — which already routes through `run_guarded_write` (the same P2 subprocess + `raw/` snapshot gate + bounded self-heal write path as `ingest`), so `query.py`/`jobs.py`/`/api/jobs/{id}` are **untouched** and there is **no structured-core refactor** (unlike heal's `run_heal_result`): the thunk returns an `int` exit code and the answer + gate receipt land in `job.output` via the worker's stdout capture, so `Job.result` stays `null` (same shape as `ingest`). `question` is `field_validator`-stripped (blank → 422); it runs through the subprocess runner (so the P4-8 embedding pitfalls don't apply, `model` = `body.model or model` may resolve to `None`); `_reject_if_writable_active()` covers the writable-turn bypass (→ 423); `on_job_done` adds `"backfill"` to the wiki-generation bump set. The frontend has **two entries** (a top-bar 「回填」 button + overlay, and a per-bubble 「沉淀」 button that pre-fills that turn's question but never copies the read-only answer — backfill is a fresh gated write, decision P4.8-3); adds no new exit codes/SSE. Guarded by `tests/test_web.py` (adapter enqueue / exit-code passthrough / blank-question 422 / `result==null` / FIFO serialization / 423 / `model` passthrough) + `tests/test_web_i18n.py` (zh/en parity). A ninth Web-host half-phase, [`docs/P4.9-只读多会话.md`](docs/P4.9-只读多会话.md) (**P4.9** — implemented, shipped in v0.1.5), opens the single-user host as a **read-only multi-user deployment** (`guanlan web --reader`) — an **E2 (multi-tenancy) session-tenancy precursor** that isolates *conversation history only* while the KB stays **fully shared read-only**, writing **no identity/auth code**. Isolation rides the existing 122-bit capability UUID (`?c=<conversation_id>`): closing the `GET /api/conversations` enumeration endpoint makes others' ids undiscoverable (capability-URL model; honest threat boundary in design §3). `create_app(reader=True)` **does not register** any write route (`raw`/`upload`/`ingest`/`heal`/`backfill`/`workspace`-delete/`GET /graph` rebuild/`chat undo` → 404/405) and **internally forces** `session_persist=False` + `mode="read-only"` (overrides args, so *any* caller — CLI/embed/test — is zero-write read-only; clamp point is `create_app` itself, not just `serve`/CLI); `/api/chat/{id}/mode` rejects `workspace-write`. **Default zero-byte KB writes** (persistence off, `agent_log` defaults off). The `agent_log` flag becomes tristate (`--agent-log`/`--no-agent-log` `BooleanOptionalAction`, default resolved in `serve`: non-reader→on / reader→off / explicit overrides) with an independent `open(<kb>/agentao.log,"a")` write-probe → startup `EXIT_USAGE` on read-only mount (`require_kb_root` only checks existence). `MAX_CONVERSATIONS` is now configurable (`--max-conversations`, default 100; `<1` → `GuanlanError(EXIT_USAGE)`, authoritative validation in `create_app`), paired with **reader-only idle reclaim** (`ConversationStore` idle TTL, `time.monotonic` injectable, evicts stale conversations with no in-flight turn under the create/restore lock). Frontend: `/api/info` gains a `reader` field driving chrome hiding (write/history-enumeration/maintenance buttons), `?c=` restore switches to a **by-id probe** `GET /api/chat/{id}/info` (never enumerates), and `/status` skips the sessions line when `conversations`/`max_conversations` are absent (trimmed under reader). Still `workers=1` + 127.0.0.1-only (decisions P4-2/P4-4 unbroken); no new exit codes/SSE/data-model changes. Guarded by `tests/test_web.py` (write-route trimming / capability isolation / idle reclaim / `max_conversations` validation / CLI tristate / KB zero-write / `create_app` direct-build clamp) + `tests/test_web_i18n.py` (zh/en parity). **P5.0 + P5.1 + P4.10 + P3.5 are now implemented** (the bold status line above is authoritative on what has shipped). [`docs/P5.0-检索层.md`](docs/P5.0-检索层.md) (**P5.0 — implemented** — `guanlan search`: a zero-LLM, **stateless** (no persisted derivative) deterministic BM25 + CJK-2-gram **full-page** recall primitive over `iter_pages` content pages, with title/alias field-boost (BM25F-lite: boost lifts `tf` only, never `dl`/`avgdl`), byte-stable output (rounded score + path tie-break); factored as a non-printing `search_pages()` core in `guanlan/search.py` for future web/mcp reuse, with `tokenize()` as the single CJK-2-gram归口 and a per-root `CorpusCache` mtime memo; opens the broadened **P5「语料规模化：多格式 + 检索」** milestone and is the **zero-infra precursor to E1**'s heavier vector/rerank/persisted-index upgrade; is query/skill's recall front-end (additive, graceful fallback to the index.md scan); vector/rerank/分词 stay deferred; no new exit codes. Guarded by `tests/test_search.py`). A tenth Web-host half-phase, [`docs/P5.1-Web检索接入.md`](docs/P5.1-Web检索接入.md) (**P5.1 — implemented** — Web 检索接入与长驻缓存: wires the P5.0 kernel into the long-lived Web process. `create_app()` builds one `app.state.search_cache = CorpusCache()` shared by **both** the new read-only `GET /api/search?q&limit` endpoint (calls `score(cache.corpus(root/"wiki"), …)` directly — **no** shell-out to the `guanlan search` CLI, off-loop via `anyio.to_thread.run_sync`, reader-available since it is non-write, decision P5.1-1/7) **and** the embedded chat's new `guanlan_search` host tool. Success body flows through a new `search.search_result_dict(result)` **single 归口** that CLI `_render`'s JSON branch also calls now — CLI/Web/(future MCP) share one field+rounding map, **field/structure-identical** (parsed-equal, not byte-equal: FastAPI compact vs CLI `indent=2`, decision P5.1-4); error body is **HTTP-native** `422 + {"detail":…}` for blank/punct query or `limit<1`, NOT the CLI `{"ok":false,…}` (decision P5.1-4/5). The `guanlan_search` tool is **`is_read_only=True`** (so agentao's `tool_planning._decide` does not DENY it under read-only — giving **read-only Web sessions their first deterministic recall entry**, since they have no shell to run the CLI: this, not "save a cold shell-out", is P5.1's core value, decision P5.1-6); its sync `execute()` returns a **JSON string** (agentao tool contract `-> str`), runs on the turn's chat-executor thread (arun's `run_in_executor`, off the event loop — does not block SSE — but does occupy that turn's executor thread to completion), self-clamps `limit` to ≥1, and is built per-`Conversation` by `make_guanlan_search_tool(search_cache, wiki=root/"wiki")` injected via构造期 `opts["extra_tools"]` on **both** new-create and lazy-restore paths — **shared cache, distinct Tool instance** (agentao binds per-agent `working_directory`/`filesystem`/`shell` onto the instance, so instances must not be reused across sessions). The shared `SKILL.md` query step-1 **and** `query.py` `QUERY_PROMPT` are made **transport-neutral** ("use the available search entry — host `guanlan_search` tool / `guanlan search` CLI / fallback to scanning") so the read-only CLI `query` dead-instruction is also fixed (decision P5.1-6); CLI fallback is reachable only in workspace-write sessions. Frontend: `#wiki-search` non-empty input debounces to `/api/search?limit=20` with a monotonic `searchToken` stale-response guard (empty input still shows the local `{kind:"index"}` directory; new `{kind:"search"}` view caches results for back/forward), decision P5.1-5. Stays inside P5.0's "markdown is sole truth, index rebuildable, no on-disk derivative" boundary — **not E1** (no vectors/rerank/persisted index); `workers=1` + 127.0.0.1-only unbroken; no new exit codes/SSE/data-model. Guarded by `tests/test_web.py` (body recall / CLI field-parity / 422 / no-hit / cache reuse + incremental / concurrency / reader-available zero-write / tool is-read-only+str / limit-clamp / extra_tools injection / shared-cache-distinct-instance / `/tools` introspection / transport-neutral prompt) + `tests/test_search.py` (`search_result_dict` 归口) + `tests/test_web_i18n.py` (zh/en parity)). [`docs/P3.5-图谱分析.md`](docs/P3.5-图谱分析.md) (**P3.5 — implemented** — zero-LLM **deterministic** graph topology deepening P3: a new `guanlan/graphstats.py`归口 over `graph.build_graph`'s adjacency computes **deterministic Louvain** communities — fixed id-order traversal + ΔQ>0-only moves + current-community-preferring smallest-rep tie-break + canonical relabel (by smallest original member) so `graph.json` stays byte-stable — surfaced *additively* as a `community` field + `stats.communities` and as community/topology notes in the零-JS `graph.html` (decision P3-7 unbroken), plus three new advisory `lint` findings `lint.hub_node`/`lint.thin_intercommunity_link`/`lint.isolated_community` on the same `g`; **renamed `fragile_bridge`→`thin_intercommunity_link`** (it detects a single direct inter-community link, NOT a graph-theoretic bridge — no delete-edge connectivity check, decision P3.5-13); `undirected_adjacency` **filters self-loops** (build_graph keeps them, decision P3.5-11) and `isolated_community` is **guarded to `>1` total communities** so a single-community small KB is never flagged a silo (decision P3.5-12); **phantom-hub ≡ existing `lint.missing_entity`** so it is NOT re-detected; **LLM-inferred edges excluded** as non-deterministic and graph-rebuildability-breaking; no new command, no new exit codes — advisory findings ride existing `EXIT_LINT_FINDINGS`. Guarded by `tests/test_graphstats.py` (determinism+byte-stability / two-clique→2-communities+thin-link / non-bridge-with-alt-path / hub σ-threshold+degree-floor / silo two-cluster+single-community-guard+orphan-exclusion / self-loop+broken-edge exclusion / empty+single-node) + `tests/test_graph.py` (additive `community`/`communities`)). [`docs/P4.10-MCP宿主.md`](docs/P4.10-MCP宿主.md) (**P4.10** — `guanlan mcp`: a read-only MCP **server** host = the P4 host-layer's **second transport (stdio)** beside Web, broadening **P4 to「可选宿主层（Web + MCP）」**, under a new `guanlan/mcp/` subpackage gated by a `guanlan-wiki[mcp]` extra (graceful degrade, mirrors decision P4-2); exposes seven read-only tools — `search`/`read_page`/`list_pages`/`graph`/`health`/`lint` (zero-LLM, reuse the same cores as Web's read endpoints) + the one LLM tool `ask` (reuses the CLI-query read-only subprocess path, so the P4-8 embedding pitfalls don't apply); **crucially does NOT touch `chat.py`** — MCP clients hold their own conversation, so the host needs no server-side session/conversation state (its biggest subtraction vs Web); mirrors the P4.9 reader **zero-byte-KB-write** posture and is the **local-read-only precursor to E2**'s remote/scoped MCP; explicitly **distinct from DESIGN's 'Tool 注入'**, which is the opposite direction — Agentao as MCP *client* consuming tools; `search` softly depends on P5.0; no new exit codes. **Implemented**: `guanlan/mcp/{__init__,server,tools}.py` — `build_mcp(root, *, model, runner, search_cache)` registers seven `async` FastMCP tools (each offloads its blocking core via `anyio.to_thread.run_sync`, decision P4.10-15) whose **return-type TypedDict annotations drive FastMCP's auto-generated output schema** → `structuredContent` + a parsed-equal JSON text block (decision P4.10-10); `tools.py` carries the thin sync impls behind a unified `@_guard` total-shell that converts any core exception to an in-band `ToolError` (server never crashes / stdio frame intact, decision P4.10-16); the `search`/`health`/`lint` envelopes reuse `search.search_result_dict` and the **new `pages.report_dict` 归口** (extracted from `report_json`, byte-identical, no trailing newline); `read_page`'s local `_safe_wiki_file` clamps traversal without importing `web`; `ask` mirrors the CLI-query read-only subprocess path with `model` = tool-arg or startup `--model` or None. CLI `guanlan mcp` (`--model`) degrades gracefully when the extra is absent (`pip install 'guanlan-wiki[mcp]'`). Guarded by `tests/test_mcp.py` (in-memory client/server round-trip: tools-list/no-write-tools, output-schema present, structured+text parsed-equal, core-parity for all six zero-LLM tools, `report_dict` byte-preservation, search→read path chain, traversal blocked, error total-shell server-survival, limit-clamp/bad-type/bad-query defenses, persistent-cache reuse + concurrency, ask cited-answer/model-resolution/ok=False-in-band, stdout-clean full suite, KB zero-write incl. ask, serve_mcp KB-validation + direction-clarified) + `tests/test_cli.py` (parser + missing-extra degrade via `sys.modules` stub)). DESIGN §7 milestone table + §8 deferred items (CJK retrieval, graph 增强) updated to point at these three.

## Commands

```bash
uv run guanlan init /tmp/demo            # scaffold a knowledge base (deterministic, zero-LLM)
uv run guanlan -C /tmp/demo check        # deterministic validation (frontmatter / broken links / sources)
uv run guanlan -C /tmp/demo health       # P3: stub pages + index↔disk sync (advisory; --strict → exit 6)
uv run guanlan -C /tmp/demo lint         # P3: orphans / broken links / missing entities (advisory)
uv run guanlan -C /tmp/demo graph        # P3: write graph/graph.json + graph.html (--json-only skips html)
uv run guanlan -C /tmp/demo reindex      # P3.4: register disk pages missing from index.md (zero-LLM; --dry-run / --prune)
uv run guanlan -C /tmp/demo web --no-browser   # P4: optional local Web host (needs guanlan-wiki[web]; 127.0.0.1 only)
uv run guanlan -C /tmp/demo mcp          # P4.10: optional read-only MCP server over stdio (needs guanlan-wiki[mcp])
uv run pytest                            # run all tests
uv run pytest tests/test_web.py          # P4 Web host tests (skipped if guanlan-wiki[web] absent)
uv run pytest tests/test_mcp.py          # P4.10 MCP host tests (skipped if guanlan-wiki[mcp] absent)
uv run pytest tests/test_init.py::test_init_is_idempotent_and_non_destructive  # single test
```

(`ingest` / `query` and the Web host's chat drive Agentao + the skill and need a configured model; `init` / `check` / `health` / `lint` / `graph` / `reindex` are the zero-LLM ones runnable offline. `guanlan web` needs the optional `web` extra: `uv pip install 'fastapi>=0.110' 'uvicorn>=0.29' 'markdown>=3'`, or `pip install 'guanlan-wiki[web]'`. `guanlan mcp` needs the optional `mcp` extra: `pip install 'guanlan-wiki[mcp]'` — a read-only MCP **server** over stdio, distinct from DESIGN's reverse-direction "Tool 注入" where Agentao is the MCP *client*.)

Python 3.12+, dependencies managed by `uv` (see `uv.lock`). The package depends on `agentao` (the governed Agent runtime executing LLM-driven `ingest`/`query` via subprocess, and — in P4 — embedded read-only for Web chat). The `web` extra (fastapi/uvicorn/markdown) is optional and not part of the core install.

## Architecture

The project deliberately separates three concerns. Internalize this split before editing — most design decisions follow from it.

1. **`guanlan/` — the thin CLI wrapper (this package).** It carries *no business intelligence*. Its only jobs are: (a) `init` (deterministic template generation, zero LLM), and (b) in P2, orchestrating Agentao + the skill and enforcing deterministic gates on write operations. `cli.py` is argparse-only; `init.py` does the file generation.

2. **`skills/guanlan-wiki/` — the maintenance engine.** This is where the actual wiki-maintenance workflows live (`SKILL.md` = workflows, `references/conventions.md` = default page/frontmatter/naming conventions, and `scripts/*.py` = deterministic checks, to be added in P2/P3). The engine is shipped/installed *once* and is **not** copied into each knowledge base. It is intended to run under Agentao's skill discovery.

3. **User knowledge base (generated by `guanlan init`).** Holds only data + per-base config: `AGENTAO.md` (Agent behavior hard-constraints + pointers), `SCHEMA.md` (this base's domain/page-types/custom rules), `raw/` (read-only sources), `wiki/` (Agent-owned generated layer: `index.md`, `log.md`, `overview.md`, plus `sources/ entities/ concepts/ syntheses/`).

### Two run modes (do not mix them)

- **Development = repo root *is* a sample wiki.** Set Agentao's `working_directory` to this repo root; `skills/guanlan-wiki/` then hits Agentao's repo-root discovery path (`<wd>/skills/`), so the engine is found with no install. Sample wiki data (`raw/`, `wiki/`, `graph/`) and the dev-copied `AGENTAO.md`/`SCHEMA.md` are `.gitignore`d — they may contain machine-local paths and never get committed.
- **External real wiki = global install.** The skill is installed to `~/.agentao/skills/guanlan-wiki/` (cwd-independent), with the user's base as `working_directory`. There is intentionally no "discover repo skills/ from an external wiki" path.

### init template duality (`guanlan/init.py`)

`init` copies a template tree. `_templates_dir()` resolves two locations by priority: bundled `guanlan/_templates/` (installed wheel) → repo-root `examples/` (development). The wheel's `force-include` in `pyproject.toml` copies `examples/{AGENTAO.md,SCHEMA.md,wiki}` into `guanlan/_templates/` at build time — so **`examples/` is the single source of truth for init templates**; edit templates there, not in `_templates/`. `init` never overwrites existing files (idempotent) and substitutes a `__DATE__` token in `wiki/` seed files.

## Invariants that drive the design

These come up repeatedly in DESIGN and the skill; preserve them in any change:

- **Markdown is the only source of truth.** Any index / graph / cache is a derivative that must be idempotently rebuildable from markdown — it never becomes authoritative.
- **`raw/` is read-only and immutable.** In P2 this is enforced *deterministically by the wrapper* via a before/after snapshot (filename + size + mtime, SHA256 if needed) around the Agentao call — not by permission config, since a snapshot also catches shell `mv`/`rm`/`python` writes that bypass `write_file`.
- **Zero-LLM scripts vs. LLM-only workflows.** Deterministic work (frontmatter/wikilink/structure checks, graph building) is plain Python scripts with no LLM. LLM is used *only* for `ingest` and `query`, and always via the Agentao runtime — scripts must never carry their own LLM client or API keys.
- **`SCHEMA.md` / `AGENTAO.md` / `index.md` / `log.md` / `overview.md` are config, not content** — exclude them from index/graph/lint scans.
- **Data conventions** (frontmatter fields, kebab-case vs TitleCase naming, `[[wikilink]]` resolution, `index.md`/`log.md` formats, the `## ⚠️ 矛盾与存疑` contradiction-marking format) are specified in `skills/guanlan-wiki/references/conventions.md` and DESIGN §4.5. A base's `SCHEMA.md` may override defaults.

## Conventions

The codebase (code comments, docstrings, design docs, user-facing CLI output) is written in **Chinese**. Match that when editing existing files.
